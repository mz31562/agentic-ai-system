# ============================================================
# AGENTIC AI SYSTEM — Python dependencies
# ============================================================

# Core
python-dotenv>=1.0.0
aiohttp>=3.9.0
aiofiles>=23.0.0
requests>=2.31.0

# API layer
fastapi==0.109.0
uvicorn[standard]==0.27.0
websockets==12.0
pydantic==2.5.3

# ============================================================
# LLM Backends — install only what you use
# ============================================================

# Groq (free tier — recommended starting point)
groq>=0.4.0

# Ollama (local/free — needs Ollama installed separately)
ollama>=0.1.0

# Anthropic Claude (paid)
anthropic>=0.25.0

# OpenAI / DALL-E (paid)
openai>=1.0.0

# HuggingFace (free inference API)
huggingface-hub>=0.19.0

# ============================================================
# Image Generation Backends — install only what you use
# ============================================================

# Replicate (free tier available)
replicate>=0.15.0

# ============================================================
# Testing
# ============================================================

pytest>=7.4.0
pytest-asyncio>=0.21.0